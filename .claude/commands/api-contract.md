---
# AI 生成:Claude Code 斜杠命令 /api-contract(适配层壳)—— 契约内容源:.ai/skills/api-contract-check/SKILL.md
description: 检查 web/server/agent-engine 三端接口调用是否与 docs/architecture.md 的 API 契约一致
argument-hint: [文件范围或留空检查 git diff]
---

先阅读 `.ai/skills/api-contract-check/SKILL.md`,严格按其中的契约表与检查步骤执行。

检查范围:$ARGUMENTS(留空则用 `git diff` 确定变更集)
