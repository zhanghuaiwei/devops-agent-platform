---
# AI 生成:Claude Code 斜杠命令 /commit(适配层壳)—— 规范内容源:.ai/skills/commit-convention/SKILL.md
description: 生成或校验符合本仓库 Conventional Commits 规约的提交信息
argument-hint: [留空则根据暂存区改动生成提交信息]
---

先阅读 `.ai/skills/commit-convention/SKILL.md`,严格按其中的格式与规则生成或校验提交信息;提交前提醒执行 `pre-commit run --all-files`。

用户输入:$ARGUMENTS
