# AI 生成:开发/测试依赖 —— CI 的 agent-engine job 安装本文件
-r requirements.txt                     # 先引入运行依赖(测试要跑真实代码)
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0               # 异步测试支持(FastAPI/SSE 测试必需)
ruff>=0.6,<0.7                          # lint + format(pre-commit 与 CI 共用)
httpx>=0.27,<1.0                        # TestClient/ASGI 测试客户端
pytest-cov>=5.0,<6.0                    # 覆盖率(门槛:agents/tools ≥ 70%)
