# AI 生成:运行依赖 —— 版本红线见 CLAUDE.md(LangChain 0.3.x / FastAPI 0.115.x)
# 锁定小版本区间,避免 0.x 依赖的破坏性变更悄悄进来
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.31.0,<0.32.0
langchain>=0.3.0,<0.4.0
langchain-openai>=1.4.1,<1.5.0        # OpenAI 兼容协议封装,通用于千问/DeepSeek
langchain-community>=0.3.0,<0.4.0     # Docker 等社区 Tool 的基础
pydantic>=2.9,<3.0                    # 数据校验(LangChain 0.3 已全面 Pydantic v2)
pydantic-settings>=2.5,<3.0           # 环境变量配置管理
httpx>=0.27,<1.0                      # 异步 HTTP 客户端(健康检查 Tool 用)
structlog>=24.4,<25.0                 # 结构化日志(替代 print,安全红线:日志脱敏)
sse-starlette>=2.1,<3.0               # FastAPI SSE 流式响应封装
docker>=7.1,<8.0                      # Docker SDK(docker_ps / docker_logs Tool)
